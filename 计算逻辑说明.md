# L220 Low RR MTM Forecast Recommendation — 计算逻辑说明

> 本文件用于记录整个推荐计算流程的业务逻辑，便于维护和调整。
> 修改参数请优先改 `config.py`；修改算法请改 `rr_recommendation.py`。
> 最后更新：2026-06-04

---

## 1. 总体目标

为**低量（Low RR）MTM/PN** 生成未来月度 RR（Replenishment/需求）推荐值，
按 GEO 拆分后通过邮件分发给各区域计划员，并生成可直接上传 BY 系统的模板。

核心思路：用 PN 在各 GEO 的**历史月度出货量**，通过**截尾平均（trimmed mean）**算出一个稳定的月度推荐值。

---

## 2. 代码结构

| 文件 | 职责 |
|------|------|
| `config.py` | 集中配置：计算参数、文件路径、邮件收件人、上传模板、调度时间 |
| `presto_conn.py` | Presto 数据库连接 + `query_df(sql)` 查询封装 |
| `rr_recommendation.py` | **核心计算模块**：取数 → 清洗 → 筛选 → 推荐值 → 导出 Excel |
| `mail_sender.py` | 按 GEO 拆分结果，Outlook / SMTP 两种方式发邮件 |
| `scheduler.py` | 调度入口：`--run-now` 立即执行 / 默认按 cron 定时执行 |
| `RR_Caculate.py` | 独立的一次性取数脚本（导出 RR_C2.xlsx，与主流程无关） |

执行链路：`scheduler.job()` → `run_recommendation()`（出 Excel）→ `send_recommendation_email()`（发邮件）

---

## 3. 数据来源

### 3.1 RR 出货数据（Presto）
表：`prd_sc_common_model.v_pcsd_order_detail_cml_brand_option`

**取数范围（`get_rr_data`）：**
- 时间：最近 **12 个月** ~ 当前月（按 `rsd` 月份）
- 过滤条件：
  - `shipping_point = 'SC03'`，`brand = 'Option'`，`site_name = 'LSSC'`
  - `order_type ∈ (Customer Order, Replenishment, Free of Charge, Internal Order)`
  - `status <> 'Order Cancelled'`
  - `product_group ∈ (TBG, LBG)`
  - 排除 `so_route = 'CXLSSC'`
  - 排除 end_customer 为 `Lenovo Information Products(Shenzhen)Co.`
  - 排除 `rsd` 年份为 `9999`
- 输出字段：`RSD_Month`、`Geo`、`SubGeo`、`PN(mtm)`、`Qty(sum line_qty)`

**Geo / SubGeo 归一化映射：**
- Geo：`EUROPE`、`META` → `EMEA`
- SubGeo：
  - `GTAP` → `region_new`
  - `HTK`、`ASEAN` → `CAPK`
  - `LJP` → `JAPAN`
  - `IN`、`INDO` → `INDIA`
  - 其他保持原值

### 3.2 PN Information 表（Excel）
路径见 `config.PN_INFO_PATH`（`ACC PN Information.xlsx`）

- 用途：
  - `Comments` 列 → 识别 **EOL / LTB / LTS** 状态
  - `Category1(BU)` 列 → 带入主表展示；**DOCK 不再排除**
  - 附加信息列带入主表：`Description`、`Category1(BU)`、`Planning`
- 列名匹配大小写/空格不敏感（见 `PN_ATTR_COLUMNS` 与 `load_pn_info`）

### 3.3 KickOff Package 表（Excel）
路径见 `config.KICKOFF_PATH`

- 用途：识别 **NPI**（新品）——`GA Date` 在最近 6 个月内的 PN
- 自动查找 PN 列（含 "orderable"+"pn"）和 GA 列（含 "launch" 或 "ga"）

---

## 4. 数据清洗（`prepare_rr` / `filter_pn_info`）

1. `RSD_Month` 转为每月 1 号的日期；`Qty` 转数值，空值补 0
2. 双重保障：截断 **超过当前月份** 的未来数据
3. `filter_pn_info`：不再按 `Category1(BU)` 排除 DOCK，所有 PN 均进入有效 PN 清单 `valid_pn`
   - 注意：EOL/LTB/LTS/NPI **不再排除**，改为在结果中用 `Remark` 列标记

---

## 5. 时间窗口（`get_time_windows`）

以**系统当前月**为基准：

| 名称 | 含义 |
|------|------|
| `current_month` | 当前月（每月 1 号） |
| `recent_4m` | 最近 4 个月（含当月） |
| `future_3m` | 未来 3 个月（含当月，共 4 个时点 range(4)） |
| `rolling_quarters` | 过去 12 个月按 3 个月一段切成 4 段：[-12,-10] [-9,-7] [-6,-4] [-3,-1] |
| `data_max_month` | 数据中最新月份（落后系统超 1 个月会告警） |

---

## 6. 核心筛选：判定 PN×GEO 是否 Qualified（`build_geo_pn_summary`）

保留所有 `valid_pn` 记录，按 **PN × GEO** 聚合后输出判断标记。筛选条件不再删除行；不符合推荐资格的行仍展示，但推荐值和未来月份用 `'-'` 占位：

| 条件 | 标志列 | 规则 |
|------|--------|------|
| 低量分组 | `RR_500_Flag` | 基于 `Avg_RollingQ` 标记 `<500` 或 `>=500`，不参与 Qualified 排除 |
| 低量参考 | `Flag_Avg_RollingQ_LT_500` | 过去 12 个月分 4 段，平均值（=总量 ÷ **有数据的段数**）< **500**，仅供参考 |
| 近期活跃 | `Flag_Recent4M_HasRR` | 最近 4 个月中至少 **`MIN_RECENT_MONTHS_WITH_RR`(默认3)** 个月有 RR |
| 历史充足 | `Flag_Enough_History` | 历史有 RR 的月份数 ≥ **4** |
| 小占比标记 | `Exclude_Geo_LowShare_When_PNHigh` | PN 全 GEO 总量 > 500 **且** 该 GEO 占比 < **3%** |
| 排除原因标记 | `Filter_Mark` | 使用可读标签：`Recent 4M RR not continuous`、`History < 4 months`、`High PN total but GEO share <3%`、`AP SubGeo share too low` |
| 推荐资格 | `Recommendation_Eligible` | `<500` 行按原排除规则判断；`>=500` 行只检查最近 4 个月是否持续有 RR |

```
Qualified = True（全部保留）
<500 Recommendation_Eligible = 条件2 且 条件3 且 (非条件4)
>=500 Recommendation_Eligible = 条件2
```

> 注：原"过去4个自然季度平均 < 500"的逻辑已被注释，改为上面的滚动 12 个月分段逻辑。
> 历史上的"条件3：6个月前是否有 RR"已取消。

---

## 7. 推荐值计算（`build_recommendation`）

对所有保留的 **PN×GEO** 进入计算流程。默认**不含当前月**（`include_current_month=False`）。
最终只有 `Recommendation_Eligible=True` 且 `Remark=NULL` 的行展示推荐值；其余行推荐值和未来月份显示 `'-'`。

### 7.1 截尾平均 `trimmed_mean`
对一组月度值排序后，**去掉最大和最小各一个**再求平均：
- 0 个值 → NaN；1 个值 → 该值；2 个值 → 普通平均；≥3 个值 → 去头去尾平均

### 7.2 非 AP 区域（GEO ≠ AP）
- 按 `PN × GEO × 月` 聚合
- **补零**：从该 PN×GEO 首次出现的月份到上个月，缺失月份补 0 参与计算
- `Recommended_RR = trimmed_mean(补零后的月度序列)`
- `Recommend_Level = "GEO"`

### 7.3 AP 区域（GEO = AP）
分两步：
1. **先算 AP 整体**：按 `PN × AP × 月` 聚合（同样补零）→ `AP_Geo_Recommended_RR = trimmed_mean(...)`
2. **再按 SubGeo 历史占比分配**：
   - `SubGeo_Share = SubGeo历史总量 ÷ AP历史总量`
   - `Recommended_RR = AP_Geo_Recommended_RR × SubGeo_Share`
   - `Recommend_Level = "SubGeo_Allocated_From_AP"`
3. **AP SubGeo 阈值标记**：
   - 对 `<500` 的 AP SubGeo 行，分配后绝对值 < `AP_SUBGEO_MIN_RECOMMENDED_RR`（config 默认 1）→ 只追加 `AP SubGeo share too low`，不移除
   - 对 `<500` 的 AP SubGeo 行，占比 < `AP_SUBGEO_MIN_SHARE`（config 默认 0.01）→ 只追加 `AP SubGeo share too low`，不移除
   - 对 `>=500` 的 AP SubGeo 行，不适用这两个阈值

### 7.4 Remark 标记（`build_remark`）
| Remark | 来源 |
|--------|------|
| `EOL/LTB` | PN Information 的 Comments 含 EOL/LTB/LTS |
| `NPI` | ① KickOff 表 GA Date 在最近 6 个月内；② RR 数据中首次出现在 6 个月内的 PN |
| `NULL` | 正常，提供推荐值 |

- **有 Remark 的行**：`Recommended_RR` 和未来 3 个月列显示 `'-'`（不给建议值）

### 7.5 输出列（Recommendation 主表）
顺序：
```
PN | Description | Category1 | Planning | Geo | SubGeo | RR_500_Flag | Filter_Mark | Recommendation_Eligible | Recommend_Level | Recommend_To
  | RR-YYYY-MM...（历史月度，灰底） | Recommended_RR（绿底） | 未来3个月 YYYY-Mon（绿底） | Remark
  | Month_Count | Hist_Min | Hist_Max | Hist_Avg
  | SubGeo_Hist_Qty | AP_Hist_Qty | SubGeo_Share | AP_Geo_Recommended_RR
```
- 数值统一四舍五入到整数；`SubGeo_Share` 保留 4 位小数

---

## 8. 上传模板（`build_ap_upload_sheets` / 通用框架）

通用注册框架 `GEO_UPLOAD_BUILDERS`，目前只注册了 **AP**。新增 GEO 见 `rr_recommendation.py` 顶部注释。

**AP 模板（BY Working 格式，按 SubGeo 分 sheet）：**
- 排除有 Remark 的 PN
- 未来 **9 个月**列：前 **4** 个月（range 中 i<4）填推荐值（或 overrides 覆盖值），后 5 个填 `'-'`
- 固定列：`Item Code、Country Group、Source(0001000835)` 等
- `overrides` 后门：`{ "PN": { "SubGeo": 值 } }`（config 中配置）

---

## 9. Excel 导出（`export_results`）

主报告 Sheet：
- `Config`（参数快照）、`Summary`（全部）、`Qualified`（仅合格）、`History_RR`、`Recommendation`
- 各 GEO 上传模板：`BY Working-{SubGeo}`
- Recommendation 上色：`RR-` 历史列灰底，`Recommended_RR` 与未来月份列绿底

输出路径：`config.OUTPUT_PATH`（`../Data/RR_Low_RR_Recommendation_{时间戳}.xlsx`）

---

## 10. 邮件分发（`send_recommendation_email`）

- 按 GEO 拆分，**每个 GEO 只收到自己的数据**（保密）
- 收件人配置：`config.GEO_RECIPIENTS`（每个 GEO 有 `enabled / to / cc`）
- 每个 GEO 生成独立附件 `RR_Recommendation_{GEO}_{日期}.xlsx`
  - 去掉 `Geo` 列；非 AP 去掉 AP 专用统计列
  - AP 额外附带各 SubGeo 的 BY Working sheet
- 可选汇总报告：`config.MAIL_TO_SUMMARY`（收完整报告）
- 发送方式：`config.MAIL_METHOD` = `outlook`（本机 Outlook COM）或 `smtp`

---

## 11. 关键可调参数（`config.py`）

| 参数 | 默认值 | 含义 |
|------|--------|------|
| `INCLUDE_CURRENT_MONTH_IN_RECOMMENDATION` | False | 推荐值计算是否含当前月 |
| `MIN_RECENT_MONTHS_WITH_RR` | 3 | 条件2：最近 4 个月中至少几个月有 RR |
| `AP_SUBGEO_MIN_RECOMMENDED_RR` | 1 | AP `<500` 行分配后绝对值标记阈值 |
| `AP_SUBGEO_MIN_SHARE` | 0.01 | AP `<500` 行分配后占比标记阈值 |
| `MAIL_METHOD` | "outlook" | 邮件发送方式 |
| `SCHEDULE_CRON_*` | mon 08:00 | 定时执行时间（每周一早 8 点） |

> ⚠️ 低量阈值 **500**、历史 **≥4 个月**、小占比 **3%** 仍会作为标记输出；当前不再通过这些条件删除行。

---

## 12. 运行方式

```bash
# 立即执行一次（计算 + 发邮件）
python scheduler.py --run-now

# 启动定时调度（需 apscheduler，按 cron 配置）
python scheduler.py

# 只跑计算不发邮件（出 Excel）
python rr_recommendation.py
```

环境变量（`presto_conn.py` 需要）：`ITcode`、`Password`、`PRESTO_CERT_PATH`
